Invoke the `salvage-project` skill in salvage mode.

Recover the real requirements from a project that went down a bad path and emit
a portable spec to rebuild simpler.

Follow the four stages in order, asking one question at a time and writing
nothing into the project until the final stage.
